version: '3.9'

services:
  urlshortener:
    image: postgres:14.5
    environment:
      POSTGRES_USER: 'test'
      POSTGRES_PASSWORD: 'test'
      POSTGRES_DB: 'tasktracker'
    ports:
      - '5434:5432'
